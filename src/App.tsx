import React, { Fragment } from 'react';
import {
  SafeAreaView,
  StyleSheet,
  Text,
} from 'react-native';

const App = () => {
  return (
    <Fragment>
      <SafeAreaView>
        <Text>MagicalTodo</Text>
      </SafeAreaView>
    </Fragment>
  );
};

const styles = StyleSheet.create({

});

export default App;
